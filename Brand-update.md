# BITE branded product film

Updated the local project at `C:\Users\crazz\OneDrive\Desktop\BITE`.

The previous chocolate video is replaced by a scroll-controlled layered product animation. Three BITE-branded wrappers open to reveal chocolate, almond and berry bars; individual ingredient cutouts move separately; the wrappers return for the final beat. This is a browser animation made from image layers, not a generated video clip.

Four matching wrappers appear in the flavor cards, Meet BITE and closing product group. The cream and blush background, photographic bar textures and floating ingredients follow the supplied reference. Other page sections and the concept bag are preserved.

Generated using the built-in image generator. Master sheets and exact prompts are saved in `BITE\Brand-assets`. The deployed local page reads 13 optimized transparent WebP assets from `BITE\dist\assets\branded`. The same master artwork and prompts are included in BITE-brand-assets.zip.

Validation completed: JavaScript syntax, all referenced local files, successful local HTTP responses, three product groups, 13 branded image layers, and removal of the old hero video. The new animation's visual pacing has not been browser-tested in this update.

Edit `dist/product-film.js` for movement and timing, `dist/product-film.css` for layout, and `dist/index.html` for hero copy and image references. The hero uses 4.2 viewport heights total with 3.2 heights of active scroll. It includes pause/resume and reduced-motion behavior. Checkout remains a concept with no purchases or payments.
